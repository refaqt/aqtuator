# Architecture (SysML)

SysML requirements, block definitions and interfaces. Not yet populated.

The requirements that measurement campaigns verify should be declared here, so that each
`measurement/results/<case>/summary.md` can cite them by name rather than restating the constraint.

See [`doqs/docs/architecture.md`](../doqs/docs/architecture.md).
