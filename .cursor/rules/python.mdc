---
description: Python layout and test habits for this repository
globs: "**/*.py, tests/**"
alwaysApply: false
---

# Python

Follow [`.agents/rules/python.md`](../../.agents/rules/python.md).

Aqtuator: run entry points from the repository root. For unit tests, set `PYTHONPATH` to the repo
root and use `python -m unittest discover -s tests -p "test*.py" -v` (see `docs/onboarding.md`).
