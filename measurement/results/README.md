# Measurement results

One folder per campaign, each with a `summary.md` reporting findings, key numbers and the
requirement verified. Heavy outputs stay in external storage and are referenced through
[`../data-index.csv`](../data-index.csv).

Findings to date are written up chronologically in [`docs/dev-log/`](../../docs/dev-log/); the
2026-05-20 entry summarises the FRF results across all three drive configurations. Migrating those
into per-campaign summaries here is outstanding work.
