# Servo configurations

ODrive S1 controller gains for the two feedback configurations compared in the
[`tap-tests`](../cases/tap-tests/) and [`stability-tests`](../cases/stability-tests/) campaigns.

These are configuration, not measurement data, so unlike the captures themselves they are committed.
Cite the relevant file from any `summary.md` that reports results for a configuration.
