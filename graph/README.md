# Graph

Generated reverse-usage graph (`usage-graph.json`), written by `doqs/doqs.py generate`.

The script behind it is `doqs/scripts/resolve_graph.py`, renamed from `build_graph.py`
in September 2026. `python doqs/scripts/resolve_graph.py --check` reports a stale file
instead of rewriting it.
