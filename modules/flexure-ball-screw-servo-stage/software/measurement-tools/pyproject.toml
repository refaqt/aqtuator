[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "measurement-tools"
version = "0.1.0"
description = "Build and verify the manifest that indexes AQTUATOR measurement data held outside Git"
requires-python = ">=3.10"
dependencies = []

[tool.setuptools.packages.find]
where = ["src"]
