# Bill of materials

Not yet populated. The root `bom/bom.csv` is generated by aggregating module BOMs and is gitignored;
source BOMs live in each module's own `bom/bom.csv`.

Part ID format and the column order are enforced — see
[`doqs/docs/naming.md`](../doqs/docs/naming.md).
